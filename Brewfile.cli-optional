# Optional CLI formulae that use external taps or are more likely to break.
# Failures from this Brewfile are reported but do not stop the full setup.

# External taps
tap "xcodesorg/made"

# Apple platform tooling
brew "xcodesorg/made/xcodes"
