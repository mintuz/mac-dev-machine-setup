# Complete package inventory shared by personal and work profiles.

# Shared CLI formulae and CLI casks
instance_eval File.read(File.expand_path("Brewfile.cli", __dir__))

# Shared GUI casks
instance_eval File.read(File.expand_path("Brewfile.gui", __dir__))

# Shared Mac App Store apps
instance_eval File.read(File.expand_path("Brewfile.app-store", __dir__))
